

services:
  web:
    build:
      dockerfile: Dockerfile
    ports:
      - "5001:5000"
    volumes:
      - ./instance:/app/instance
    environment:
      - FLASK_APP=main.py
      - FLASK_ENV=development